# Client Environment Configuration
# Override these variables in production deployment environments.

# Optional: Custom backend API base URL (defaults to /api which proxies to http://localhost:5000 in development)
VITE_API_BASE_URL=

# Optional: Custom Socket.IO server URL (defaults to http://localhost:5000 in development)
VITE_SOCKET_URL=
